# Procscope Environment Configuration

# API Authentication Token
# Required for process kill endpoint
# Generate a secure random token: openssl rand -hex 32
PROCSCOPE_API_TOKEN=your-secure-token-here

# Server Configuration
PORT=3000
HOST=0.0.0.0
NODE_ENV=development
